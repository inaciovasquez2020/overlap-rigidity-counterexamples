      - name: Build PDFs
        run: |
          shopt -s nullglob globstar
          files=(**/*.tex)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No .tex files found; skipping PDF build."
            exit 0
          fi
          latexmk -pdf -interaction=nonstopmode -halt-on-error "${files[@]}"

